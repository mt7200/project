# 中医医疗系统 - 技术约束清单

> 基于现有项目代码和数据库设计文档整理

---

## 一、技术栈选型

| 层级 | 技术选型 | 版本 | 说明 |
|------|----------|------|------|
| **前端框架** | Vue | 3.5+ | TypeScript + Composition API |
| **构建工具** | Vite | 8.x | ESBuild 加速打包 |
| **路由管理** | Vue Router | 4.5+ | 支持嵌套路由 |
| **图表库** | ECharts | 5.x | 数据可视化 |
| **编程语言** | TypeScript | 6.x | 严格模式 `strict: true` |
| **HTTP 客户端** | axios | 待添加 | 封装请求拦截器 |
| **后端语言** | Python | 3.10+ | 推荐使用 |
| **后端框架** | FastAPI | 最新 | 异步高性能，自动 API 文档 |
| **ORM 框架** | SQLAlchemy | 2.x | 对应 Python |
| **数据库** | MySQL | 8.x | InnoDB 引擎 |

---

## 二、前端详细约束

### 2.1 TypeScript 配置

```json
{
  "target": "ES2020",
  "module": "ESNext",
  "strict": true,
  "noUnusedLocals": false,
  "noUnusedParameters": false,
  "skipLibCheck": true,
  "moduleResolution": "bundler",
  "allowImportingTsExtensions": true,
  "resolveJsonModule": true,
  "isolatedModules": true,
  "noEmit": true,
  "jsx": "preserve",
  "jsxImportSource": "vue",
  "baseUrl": ".",
  "paths": {
    "@/*": ["src/*"]
  },
  "include": ["src"]
}
```

### 2.2 前端核心文件结构

| 文件/目录 | 说明 |
|-----------|------|
| `src/main.ts` | 应用入口 |
| `src/App.vue` | 根组件 |
| `src/router/index.ts` | 路由配置（Vue Router v4） |
| `src/menus.ts` | 侧边栏菜单 |
| `src/components/Layout.vue` | 布局组件 |
| `src/pages/Login.vue` | 登录页 |
| `src/pages/PatientEntry.vue` | 患者入院登记 |
| `src/pages/Workspace.vue` | 四诊信息录入 |
| `src/pages/DiagnosisFlow.vue` | 辨证诊断流程 |
| `src/pages/PrescriptionFlow.vue` | 开方流程 |
| `src/pages/PrescriptionReview.vue` | 处方审核 |
| `src/pages/EMRViewEdit.vue` | 电子病历编辑查看 |
| `src/pages/Statistics.vue` | 数据统计面板 |
| `src/pages/HistoryRecords.vue` | 历史病历档案 |
| `src/pages/Diagnosis.vue` | 诊断页面 |
| `src/pages/IntelligentPrescription.vue` | 智能开方 |
| `src/pages/AISidebar.vue` | AI 侧边栏 |

### 2.3 路由定义

```typescript
// router/index.ts 路由结构
import { createRouter, createWebHistory } from 'vue-router'

const router = createRouter({
  history: createWebHistory(),
  routes: [
    { path: '/login', component: () => import('@/pages/Login.vue') },
    {
      path: '/',
      component: () => import('@/components/Layout.vue'),
      children: [
        { path: '', component: () => import('@/pages/DiagnosisFlow.vue') },
        { path: 'diagnosis-flow', component: () => import('@/pages/DiagnosisFlow.vue') },
        { path: 'prescription-flow', component: () => import('@/pages/PrescriptionFlow.vue') },
        { path: 'emr', component: () => import('@/pages/EMRViewEdit.vue') },
        { path: 'statistics', component: () => import('@/pages/Statistics.vue') }
      ]
    }
  ]
})

export default router
```

---

## 三、Python 后端技术约束

### 3.1 FastAPI 项目结构推荐

```
backend/
├── app/
│   ├── __init__.py
│   ├── main.py              # 应用入口
│   ├── config.py            # 配置文件
│   ├── database.py          # 数据库连接
│   ├── models/              # SQLAlchemy 模型
│   │   ├── user.py
│   │   ├── patient.py
│   │   ├── visit_record.py
│   │   ├── diagnosis_result.py
│   │   ├── herb.py
│   │   ├── herb_incompatibility.py
│   │   ├── formula.py
│   │   ├── formula_herb.py
│   │   ├── prescription.py
│   │   ├── prescription_item.py
│   │   ├── prescription_review.py
│   │   ├── emr.py
│   │   ├── syndrome_pattern.py
│   │   └── symptom_dict.py
│   ├── schemas/             # Pydantic 模型（请求/响应 DTO）
│   │   ├── auth.py
│   │   ├── patient.py
│   │   ├── diagnosis.py
│   │   ├── prescription.py
│   │   ├── herb.py
│   │   └── statistics.py
│   ├── api/                 # API 路由
│   │   ├── __init__.py
│   │   ├── auth.py          # 认证接口
│   │   ├── patients.py      # 患者 CRUD
│   │   ├── visits.py        # 就诊记录
│   │   ├── diagnoses.py     # 诊断结果
│   │   ├── herbs.py         # 药材查询
│   │   ├── prescriptions.py # 处方/审方
│   │   └── statistics.py    # 统计数据
│   ├── core/                # 核心模块
│   │   ├── security.py      # JWT 认证
│   │   └── exceptions.py    # 自定义异常
│   └── services/            # 业务逻辑层
│       ├── herb_service.py
│       ├── prescription_service.py
│       └── review_service.py
├── requirements.txt
└── .env
```

### 3.2 Python 依赖包 (requirements.txt)

```txt
fastapi==0.115.0
uvicorn[standard]==0.32.0
sqlalchemy==2.0.35
pymysql==1.1.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.18
pydantic==2.10.0
python-dotenv==1.0.1
alembic==1.14.0
```

### 3.3 FastAPI 核心代码模板

```python
# app/main.py
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from app.api import auth, patients, visits, diagnoses, herbs, prescriptions, statistics

app = FastAPI(
    title="中医医疗系统 API",
    description="基于 FastAPI 的后端服务",
    version="1.0.0"
)

# 允许跨域
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

# 注册路由
app.include_router(auth.router, prefix="/api/auth", tags=["认证"])
app.include_router(patients.router, prefix="/api/patients", tags=["患者管理"])
app.include_router(visits.router, prefix="/api/visits", tags=["就诊记录"])
app.include_router(diagnoses.router, prefix="/api/diagnoses", tags=["辨证诊断"])
app.include_router(herbs.router, prefix="/api/herbs", tags=["中药材"])
app.include_router(prescriptions.router, prefix="/api/prescriptions", tags=["处方/审方"])
app.include_router(statistics.router, prefix="/api/statistics", tags=["数据统计"])
```

### 3.4 SQLAlchemy ORM 基础类

```python
# app/database.py
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker, declarative_base
from app.config import settings

engine = create_engine(
    settings.DB_URL,
    pool_pre_ping=True,
    pool_size=10,
    max_overflow=20
)

SessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)
Base = declarative_base()

def get_db():
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()
```

### 3.5 Pydantic DTO 示例

```python
# app/schemas/prescription.py
from pydantic import BaseModel, Field
from typing import Optional, List
from datetime import datetime

class PrescriptionItemCreate(BaseModel):
    herb_id: int = Field(..., description="药材 ID")
    dosage: float = Field(..., ge=0, description="实际用量")
    unit: str = Field(default="g", description="单位")

class PrescriptionCreate(BaseModel):
    patient_id: int = Field(..., description="患者 ID")
    doctor_id: int = Field(..., description="医生 ID")
    formula_id: Optional[int] = Field(None, description="基于方剂 ID")
    diagnosis: str = Field(..., max_length=200, description="诊断")
    syndrome: str = Field(..., max_length=200, description="证型")
    notes: Optional[str] = Field(None, description="医嘱备注")
    items: List[PrescriptionItemCreate] = Field(..., description="处方药材明细")

class PrescriptionResponse(BaseModel):
    id: int
    prescription_no: str
    patient_id: int
    doctor_id: int
    formula_id: Optional[int]
    diagnosis: str
    syndrome: str
    status: str
    created_at: datetime

    class Config:
        from_attributes = True
```

### 3.6 JWT 认证中间件

```python
# app/core/security.py
from datetime import datetime, timedelta
from typing import Optional
from jose import JWTError, jwt
from passlib.context import CryptContext
from fastapi import Depends, HTTPException, status
from fastapi.security import OAuth2PasswordBearer
from sqlalchemy.orm import Session
from app.database import get_db
from app.models.user import User

pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")
oauth2_scheme = OAuth2PasswordBearer(tokenUrl="api/auth/login")

SECRET_KEY = "your-secret-key-change-this"
ALGORITHM = "HS256"
ACCESS_TOKEN_EXPIRE_MINUTES = 30 * 24  # 30 天

def verify_password(plain_password: str, hashed_password: str) -> bool:
    return pwd_context.verify(plain_password, hashed_password)

def get_password_hash(password: str) -> str:
    return pwd_context.hash(password)

def create_access_token(data: dict, expires_delta: Optional[timedelta] = None) -> str:
    to_encode = data.copy()
    expire = datetime.utcnow() + (expires_delta or timedelta(minutes=15))
    to_encode.update({"exp": expire})
    return jwt.encode(to_encode, SECRET_KEY, algorithm=ALGORITHM)

async def get_current_user(
    token: str = Depends(oauth2_scheme),
    db: Session = Depends(get_db)
) -> User:
    credentials_exception = HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Could not validate credentials",
        headers={"WWW-Authenticate": "Bearer"},
    )
    payload = jwt.decode(token, SECRET_KEY, algorithms=[ALGORITHM])
    user_id: int = payload.get("sub")
    if user_id is None:
        raise credentials_exception
    user = db.query(User).filter(User.id == user_id).first()
    if user is None:
        raise credentials_exception
    return user
```

---

## 四、数据库表约束

| 表名 | 主键 | 唯一约束 | 外键关系 | 关键字段类型 |
|------|------|----------|----------|--------------|
| `sys_user` | `id BIGINT PK` | `username VARCHAR(50) UNIQUE` | - | password_hash TEXT |
| `patient` | `id BIGINT PK` | `phone VARCHAR(20) UNIQUE` | - | name VARCHAR(50), age INT |
| `visit_record` | `id BIGINT PK` | - | → patient(id), sys_user(id) | visit_date DATE, symptoms TEXT |
| `diagnosis_result` | `id BIGINT PK` | - | → visit_record(id) | pattern_name VARCHAR(100), confidence INT |
| `herb` | `id BIGINT PK` | `name VARCHAR(50) UNIQUE` | - | nature VARCHAR(20), taste VARCHAR(20), min_dosage DECIMAL, max_dosage DECIMAL |
| `herb_incompatibility` | `id BIGINT PK` | `(herb_a_id, herb_b_id, type) UNIQUE` | → herb(id) ×2 | type ENUM('incompatibility','synergy') |
| `formula` | `id BIGINT PK` | `name VARCHAR(100) UNIQUE` | - | category_l1 VARCHAR(50), functions TEXT, indications TEXT |
| `formula_herb` | `id BIGINT PK` | `(formula_id, herb_id) UNIQUE` | → formula(id), herb(id) | dosage VARCHAR(20), sort_order INT |
| `prescription` | `id BIGINT PK` | `prescription_no VARCHAR(50) UNIQUE` | → patient(id), visit_record(id), sys_user(id), formula(id) | diagnosis VARCHAR(200), status VARCHAR(20) |
| `prescription_item` | `id BIGINT PK` | - | → prescription(id), herb(id) | dosage DECIMAL(5,1), sort_order INT |
| `prescription_review` | `id BIGINT PK` | - | → prescription(id), sys_user(id) | risk_level VARCHAR(10), review_status VARCHAR(20) |
| `emr` | `id BIGINT PK` | - | → patient(id), visit_record(id), prescription(id), sys_user(id) | chief_complaint TEXT, syndrome VARCHAR(200), treatment_principle VARCHAR(200) |
| `syndrome_pattern` | `id BIGINT PK` | `pattern_name VARCHAR(100) UNIQUE` | - | etiology VARCHAR(200), treatment_method VARCHAR(200) |
| `symptom_dict` | `id BIGINT PK` | `(category, label) UNIQUE` | - | sub_category VARCHAR(50), sort_order INT |
| `agent_log` | `id BIGINT PK` | - | → sys_user(id), visit_record(id) | stage VARCHAR(30), status VARCHAR(20) |

### 索引建议

| 表名 | 索引字段 | 用途 |
|------|----------|------|
| `visit_record` | `patient_id`, `visit_date` | 按患者查询历史记录 |
| `prescription` | `patient_id`, `created_at` | 患者处方统计 |
| `prescription_item` | `prescription_id` | 处方药材查询 |
| `emr` | `treatment_result`, `visit_date` | 疗效统计分析 |
| `diagnosis_result` | `pattern_name`, `visit_id` | 证型分布统计 |

---

## 五、业务逻辑约束

### 5.1 处方剂量校验规则

| 校验项 | 规则 | 风险等级 |
|--------|------|----------|
| 最小剂量 | `dosage ≥ herb.min_dosage` | warning |
| 最大剂量 | `dosage ≤ herb.max_dosage` | high |
| 超量比例 | `dosage > max_dosage * 1.5` | critical |

### 5.2 配伍禁忌检测规则

```python
# 十八反/十九畏检查
INCOMPATIBLE_PAIRS = {
    ('甘草', '甘遂'), ('甘草', '大戟'), ('甘草', '海藻'),
    ('乌头', '半夏'), ('乌头', '贝母'), ('乌头', '瓜蒌'),
    # ... 其他相反而配对
}

def check_incompatibility(selected_herbs: List[str]) -> List[dict]:
    conflicts = []
    for i in range(len(selected_herbs)):
        for j in range(i + 1, len(selected_herbs)):
            pair = tuple(sorted([selected_herbs[i], selected_herbs[j]]))
            if pair in INCOMPATIBLE_PAIRS:
                conflicts.append({
                    'herb_a': pair[0],
                    'herb_b': pair[1],
                    'type': '十八反'
                })
    return conflicts
```

### 5.3 审核流程状态机

```
draft → pending → approved/rejected
         ↓
       (驳回后可重新提交 pending)
```

### 5.4 证型编码规范

```
pattern_code 格式：B + 两位大类 + 两位小类
例如："B01.02" = 肝系病证 - 第 2 号子项
```

---

## 六、API 接口规范

### 6.1 统一响应格式

```json
{
  "code": 200,
  "message": "success",
  "data": {...},
  "timestamp": "2026-07-17T10:30:00Z"
}
```

### 6.2 RESTful 接口列表

| 方法 | 路径 | 描述 | 权限 |
|------|------|------|------|
| POST | `/api/auth/login` | 用户登录 | 公开 |
| GET | `/api/auth/me` | 获取当前用户信息 | 需认证 |
| GET | `/api/patients` | 获取患者列表 | 需认证 |
| POST | `/api/patients` | 新增患者 | 需认证 |
| PUT | `/api/patients/{id}` | 更新患者信息 | 需认证 |
| GET | `/api/visits` | 获取就诊记录 | 需认证 |
| POST | `/api/visits` | 创建就诊记录 | 需认证 |
| GET | `/api/diagnoses?visit_id={id}` | 获取辨证结果 | 需认证 |
| POST | `/api/diagnoses` | 提交辨证结果 | 需认证 |
| GET | `/api/herbs` | 搜索药材 | 公开 |
| GET | `/api/herbs?keyword=&category=` | 条件搜索药材 | 公开 |
| GET | `/api/formulas` | 获取方剂列表 | 公开 |
| POST | `/api/prescriptions` | 创建处方 | 需认证 |
| GET | `/api/prescriptions/{id}` | 获取处方详情 | 需认证 |
| POST | `/api/prescription-review` | 提交审核 | 需认证 |
| PUT | `/api/prescription-review/{id}` | 审核通过/驳回 | 需认证 |
| GET | `/api/emrs` | 获取病历列表 | 需认证 |
| GET | `/api/emrs/{id}` | 获取病历详情 | 需认证 |
| PUT | `/api/emrs/{id}` | 更新病历 | 需认证 |
| GET | `/api/statistics/visit-count` | 月度就诊量统计 | 需认证 |
| GET | `/api/statistics/syndrome-distribution` | 证型分布 | 需认证 |
| GET | `/api/statistics/herb-ranking` | 药材使用排名 | 需认证 |
| GET | `/api/statistics/treatment-results` | 疗效统计 | 需认证 |

---

## 七、前端页面与 API 映射

| 页面 | 必需 API | 可选 API |
|------|----------|----------|
| `Login.vue` | `POST /api/auth/login` | - |
| `PatientEntry.vue` | `GET /api/patients`, `POST /api/patients` | - |
| `Workspace.vue` | `POST /api/visits`, `GET /api/symptom-dict` | - |
| `DiagnosisFlow.vue` | `POST /api/diagnoses`, `GET /api/diagnoses` | `GET /api/syndrome-patterns` |
| `PrescriptionFlow.vue` | `GET /api/herbs`, `POST /api/prescriptions` | `GET /api/formulas` |
| `PrescriptionReview.vue` | `POST /api/prescription-review`, `PUT /api/prescription-review/{id}` | - |
| `EMRViewEdit.vue` | `GET/PUT /api/emrs` | `GET /api/syndrome-patterns` |
| `Statistics.vue` | `GET /api/statistics/*` | - |

---

## 八、团队分工约束（2 后端 + 1 前端）

| 人员 | 负责模块 | API 交付 | 协作点 |
|------|----------|----------|--------|
| **李鮋** | 数据库 + 核心 CRUD API | patients/, visits/, herbs/, formulas/, emrs/, symptom-dict/, syndrome-patterns/ | 建表 + 基础 CRUD，不依赖他人 |
| **沈文博** | 业务 API + 服务层 | auth/, prescriptions/, review/, statistics/ | 剂量校验/配伍禁忌/风险评分服务 |
| **前端 (Vue 3 + TypeScript)** | 所有页面改造、API 封装、路由守卫、组件复用 | 调用全部 API | 需要完整 API 文档和 Swagger 地址 |

---

## 九、3 天开发计划（排除 Agent/知识图谱）

| 天数 | 李鮋 (数据库+CRUD API) | 沈文博 (业务 API+ 服务层) | 前端 | 联调 |
|------|------------------------|--------------------------|------|------|
| **Day 1** | FastAPI 脚手架<br>MySQL 建表 (全部 14 张表) | JWT 认证模块准备<br>核心安全模块 | axios 封装<br>类型定义 (User/Patient/Visit)<br>Login.vue mock 改造 | 无 |
| **Day 2** | 上午：herb 表 + 药材导入 + patients CRUD<br>下午：formula/formula_herb导入 + herbs API | 上午：auth/login API + JWT 中间件<br>下午：剂量校验引擎 + 配伍禁忌检测逻辑 | PatientEntry.vue<br>Workspace.vue<br>Login.vue 对接真实 API | 患者管理 + 登录 |
| **Day 3** | 上午：visit_record CRUD + syndrome_pattern/symptom_dict 导入<br>下午：visits GET/query API + 知识库 APIs | 上午：prescription POST 创建 + prescription_item 管理 API<br>下午：集成校验服务到处方 API | DiagnosisFlow.vue<br>PrescriptionFlow.vue<br>完整开方流程联调 | 就诊 + 开方流程 |
| **Day 4** | 上午：emr CRUD + emrs API<br>下午：emrs GET/筛选/详情 | 上午：prescription_review API + statistics 聚合查询 API<br>下午：优化审核 API + 统计性能 | EMRViewEdit.vue<br>Statistics.vue<br>PrescriptionReview.vue | 病历 + 审方 + 统计 |

> Day 4 为额外缓冲日，用于全链路联调和 Bug 修复

---

## 十、已排除模块（不在 3 天计划内）

| 模块 | 数据库表 | 原因 |
|------|----------|------|
| **Agent AI 诊断** | `agent_log` | 需要 AI 模型集成，复杂度高 |
| **知识图谱** | `knowledge_graph_node`, `knowledge_graph_edge` | 扩展预留，非核心需求 |
| **舌诊图片识别** | `tongue_image` | 需要图像识别服务 |

---

## 十一、环境要求

| 项 | 最低要求 | 推荐配置 |
|----|----------|----------|
| Node.js | 18.x | 20.x LTS |
| Python | 3.10 | 3.12 |
| MySQL | 8.0 | 8.0+ InnoDB |
| IDE | VS Code | VS Code + PyCharm |
| Git | 任意 | Git 版本控制 |

---

> 文档生成日期：2026-07-16  
> 适用项目：中医医疗系统 v1.0 
